## Package size

The installed package is approximately 31 MB because it intentionally includes
the documented `clean_clea` and `simple_systems` datasets. The source datasets
use xz compression. The raw CLEA distribution is not included in the source
package, built package, repository tree, or repository history.
